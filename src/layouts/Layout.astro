---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;

import ScrollProgress from '../components/ScrollProgress.astro';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />

    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />

    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://victxrlarixs.com/" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="https://victxrlarixs.com/og-image.png" />

    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content="https://victxrlarixs.com/" />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content="https://victxrlarixs.com/og-image.png" />

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Outfit:wght@300;400;500;700;800;900&display=swap" rel="stylesheet">

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css"
    />
  </head>
  <body class="bg-slate-50 text-slate-900 font-inter selection:bg-violet-100 selection:text-violet-900 overflow-x-hidden">
    <div class="fixed inset-0 -z-10 overflow-hidden pointer-events-none" aria-hidden="true">
      <div class="absolute top-[-15%] left-[-15%] w-[60%] h-[60%] rounded-full bg-violet-300/50 blur-[100px] animate-ambient-1"></div>
      <div class="absolute bottom-[-15%] right-[-15%] w-[55%] h-[55%] rounded-full bg-blue-300/40 blur-[100px] animate-ambient-2"></div>
      <div class="absolute top-[30%] left-[25%] w-[50%] h-[50%] rounded-full bg-fuchsia-200/35 blur-[120px] animate-ambient-3"></div>
    </div>

    <ScrollProgress />

    <slot />
  </body>
</html>

<style is:global>
  @import "../styles/global.css";

  @keyframes ambient-1 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(10%, 10%) scale(1.1); }
    66% { transform: translate(-5%, 15%) scale(0.9); }
  }
  
  @keyframes ambient-2 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-15%, -5%) scale(0.9); }
    66% { transform: translate(5%, -15%) scale(1.1); }
  }

  .animate-ambient-1 { animation: ambient-1 20s ease-in-out infinite; }
  .animate-ambient-2 { animation: ambient-2 25s ease-in-out infinite; }

  @keyframes ambient-3 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    40% { transform: translate(8%, -12%) scale(1.15); }
    70% { transform: translate(-10%, 8%) scale(0.88); }
  }
  .animate-ambient-3 { animation: ambient-3 30s ease-in-out infinite; }

  h1, h2, h3, h4, h5, h6 {
    font-family: 'Outfit', sans-serif;
  }
</style>
